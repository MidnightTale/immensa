# Line endings: shell scripts stay LF, Windows batch stays CRLF.
gradlew text eol=lf
*.bat text eol=crlf

# Binary assets: never let Git touch line endings inside these.
*.jar binary
*.png binary
*.onnx binary
